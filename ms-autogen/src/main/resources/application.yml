server:
  port: 8081
spring:
  cloud:
    stream:
      bindings:
        test-messages:
          destination: tst-msg
          group: test-group
          contentType: application/json

logging:
  file: ./logs/ms-autogen.log
  level:
    root: INFO
    ru.alfacapital: DEBUG
